// console.log(); //global object

// setTimeout(); //sets a timer after which function will load
// clearTimeout(); //clears the timer

// setInterval(); //sets a time gap after which the function will run
// clearInterval(); //to stop the function called repeatedly

// window.console.log(); //this is how we access in browsers

var message = "";

console.log(global.message);

console.log(module);
